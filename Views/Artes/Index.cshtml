@model IEnumerable<ProyectoFinal_Cartagena_Zumarraga.Models.Arte>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AsuntoArt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CuerpoArt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaArt)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AsuntoArt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CuerpoArt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaArt)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
